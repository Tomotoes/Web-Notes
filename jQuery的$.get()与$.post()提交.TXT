【1】$.get() 方法使用GET方法来异步请求的。
	 $.post() 方法使用POST方法来异步请求的。
	 
【2】get请求会将参数跟在URL后进行传递，
	而POST请求 则是作为HTTP消息的实体内容发送给Web服务器，这种传递对用户不可见的。

【3】get方式传输的数据大小不能超过 2KB ， 而POST 要大的多

【4】get方式请求的数据会被游览器缓存起来，因此有安全问题